# Reviewer Notes

The Sanskrit decoder sprint adds a deterministic decoder endpoint and a governed response payload to the existing UniGuru runtime. The implementation is replay-safe and evidence-classified, and it does not rely on external APIs.
