# Isha Sanskrit Decoder Self Validation

Local validation passed for six-source registry loading, canonical schema compatibility, unknown-term no-inference, invalid API input, source evidence classification, graph consistency, ecosystem Bucket/InsightFlow path, and replay stability.

The decoder correctly records that cross-text source passages are unavailable. It does not claim production deployment validation.
